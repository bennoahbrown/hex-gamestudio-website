export const metadata = {
  title: "Cash Clock Demo | Hex Game Studio",
  description:
    "Operator demo for Cash Clock, a time-based eInstant game by Hex Game Studio.",
  robots: {
    index: false,
    follow: false,
  },
};

export default function CashClockDemoLayout({ children }) {
  return <>{children}</>;
}
