// src/app/studio/page.js
import SignalCard from "@/components/SignalCard";

export default function Studio() {
  return (
    <main
      className="min-h-screen"
      style={{ background: "var(--bg)", color: "var(--fg)" }}
    >
      {/* HERO BAND (dark, atmospheric, same vibe as homepage) */}
      <section className="relative overflow-hidden pt-28">
        {/* background wash */}
        <div className="pointer-events-none absolute inset-0">
          <div
            className="absolute inset-0"
            style={{
              background:
                "radial-gradient(900px 600px at 70% 35%, rgba(0,255,140,0.12), transparent 60%), radial-gradient(800px 500px at 20% 80%, rgba(120,0,255,0.12), transparent 60%), linear-gradient(to bottom, rgba(0,0,0,0.70), rgba(0,0,0,0.88))",
            }}
          />
          <div
            className="absolute inset-0"
            style={{
              boxShadow: "inset 0 0 160px rgba(0,0,0,0.78)",
            }}
          />
          {/* subtle grid/texture */}
          <div
            className="absolute inset-0 opacity-[0.25]"
            style={{
              background:
                "repeating-linear-gradient(to right, rgba(255,255,255,0.06) 0px, rgba(255,255,255,0.06) 1px, transparent 1px, transparent 92px), repeating-linear-gradient(to bottom, rgba(255,255,255,0.045) 0px, rgba(255,255,255,0.045) 1px, transparent 1px, transparent 74px)",
            }}
          />
        </div>

        <div className="relative mx-auto max-w-6xl px-6 pb-16">
          {/* Removed: top eyebrow bar */}

          {/* big title block */}
          <div className="mt-6 grid gap-10 lg:grid-cols-12 lg:items-end">
            <div className="lg:col-span-7">
              {/* Headline: ONLY "THE STUDIO" */}
              <h1 className="text-[clamp(52px,6.6vw,92px)] font-extrabold leading-[0.92] tracking-[-0.02em] uppercase text-[#e6dfc2]">
                THE STUDIO
              </h1>

              {/* outlined stack */}
              <div className="mt-8 space-y-2">
                {["ORIGINAL MECHANICS", "INTUITIVE SYSTEMS", "BUILT FOR LONGEVITY"].map((t) => (
                  <div
                    key={t}
                    className="text-3xl font-semibold tracking-[0.02em] sm:text-4xl"
                    style={{
                      color: "transparent",
                      WebkitTextStroke: "1px rgba(0,255,140,0.55)",
                    }}
                  >
                    {t}
                  </div>
                ))}
              </div>

              <p
                className="mt-10 max-w-xl text-lg leading-8"
                style={{ color: "rgba(255,255,255,0.78)" }}
              >
                Hex Game Studio is an independent iLottery game developer and design house focused on building original, concept-driven mechanics.
              </p>
              <p
                className="mt-4 max-w-xl text-lg leading-8 font-semibold"
                style={{ color: "rgba(255,255,255,0.90)" }}
              >
                We design games to last — not to launch and disappear.
              </p>
            </div>

            {/* right-side coordinate/system callout */}
            <div className="lg:col-span-5">
              <div
                className="border p-8"
                style={{
                  borderRadius: 0,
                  background: "rgba(255,255,255,0.06)",
                  borderColor: "rgba(255,255,255,0.14)",
                  backdropFilter: "blur(10px)",
                }}
              >
                <div
                  className="text-xs font-medium tracking-[0.28em]"
                  style={{ color: "rgba(255,255,255,0.70)" }}
                >
                  SYSTEM MARK
                </div>

                <div
                  className="mt-4 font-mono text-5xl tracking-[0.14em] sm:text-6xl"
                  style={{ color: "rgba(255,255,255,0.90)" }}
                >
                  00:00
                </div>

                <p
                  className="mt-6 text-sm leading-6"
                  style={{ color: "rgba(255,255,255,0.70)" }}
                >
                  Time as a universal human driver—intervals, anticipation, intuitive understanding. Designed for compliance and sustained engagement.
                </p>

                <div className="mt-6 grid gap-2 text-xs tracking-[0.24em]">
                  <div style={{ color: "var(--accent)" }}>
                    40.7128° N, 74.0060° W
                  </div>
                  <div style={{ color: "var(--accent)" }}>
                    34.0549° N, 118.2426° W
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* hard cut into white slab */}
        <div
          className="h-10 w-full"
          style={{
            background:
              "linear-gradient(to bottom, rgba(0,0,0,0.0), rgba(0,0,0,0.55))",
          }}
        />
      </section>

      {/* WHITE SLAB CONTENT (editorial like the About page) */}
      <section
        className="px-6 py-16 sm:py-20"
        style={{ background: "var(--paper)", color: "var(--paperText)" }}
      >
        <div className="mx-auto max-w-6xl">
          {/* lead paragraph row */}
          <div className="grid gap-10 lg:grid-cols-12 lg:items-start">
            <div className="lg:col-span-7">
              <div className="flex items-start gap-4">
                <span
                  className="mt-2 inline-block h-3 w-3"
                  style={{ borderRadius: 0, background: "var(--accent)" }}
                />
                <p className="text-2xl font-semibold leading-9 tracking-tight sm:text-3xl">
                  Most iLottery Content Is Interchangeable
                </p>
              </div>

              <p
                className="mt-8 text-lg leading-8"
                style={{ color: "var(--paperMuted)" }}
              >
                The majority of iLottery games in the market are reskins of the same underlying mechanics. They differ visually, but play identically.
              </p>
              <p
                className="mt-6 text-lg leading-8"
                style={{ color: "var(--paperMuted)" }}
              >
                This approach prioritizes volume and speed over originality, intuition, and longevity — resulting in player fatigue and short lifecycle value.
              </p>
            </div>

            <div className="lg:col-span-5">
              <div className="space-y-6">
                {[
                  { k: "Innovation Comes From Mechanics", v: "Players don't form long-term relationships with symbols. They engage with systems they intuitively understand." },
                  {
                    k: "We Don't Reskin. We Innovate.",
                    v: "Every Hex game follows the same core principle: A universal human concept becomes the mechanic itself.",
                  },
                  {
                    k: "Compliance By Design",
                    v: "Original does not mean risky. Compliance is integrated at the earliest stage of design.",
                  },
                ].map((x) => (
                  <div key={x.k} className="flex items-start gap-4">
                    <span
                      className="mt-2 inline-block h-3 w-3"
                      style={{ borderRadius: 0, background: "var(--accent)" }}
                    />
                    <div>
                      <div className="text-lg font-semibold">{x.k}</div>
                      <div
                        className="mt-1 text-base leading-7"
                        style={{ color: "var(--paperMuted)" }}
                      >
                        {x.v}
                      </div>
                    </div>
                  </div>
                ))}
              </div>
            </div>
          </div>

          {/* divider */}
          <div
            className="my-16 h-px w-full"
            style={{ background: "var(--paperBorder)" }}
          />

          {/* =========================
              HOW WE BUILD (STEP 1 replacement)
              Dark/glitch grid, square corners
             ========================= */}
          <section
            className="px-0 py-0"
            style={{
              background: "transparent",
            }}
          >
            <div
              className="py-16 sm:py-20"
              style={{ background: "rgba(0,0,0,0.88)" }}
            >
              <div className="mx-auto max-w-6xl">
                <div className="grid gap-10 lg:grid-cols-12 lg:items-start">
                  <div className="lg:col-span-4">
                    <h2 className="glitch-title text-4xl font-extrabold uppercase tracking-[-0.02em] sm:text-5xl">
                      OUR STUDIO MODEL
                    </h2>

                    <p className="mt-6 max-w-sm text-lg leading-8 text-[var(--muted)]">
                      Built to Scale Beyond a Single Title. Hex operates as a modular game studio.
                    </p>

                    <div className="mt-8 text-xs tracking-[0.28em] text-[var(--muted-2)]">
                      ORIGINAL · INTUITIVE · LASTING
                    </div>
                  </div>

                  <div className="lg:col-span-8">
                    <div className="grid gap-5 sm:grid-cols-2">
                      {[
                        {
                          n: "01",
                          title: "Identify Universal Driver",
                          desc: "Find a human concept with intuitive understanding and emotional resonance.",
                        },
                        {
                          n: "02",
                          title: "Design Compliant Mechanic",
                          desc: "Build defensible lottery systems that regulators can understand.",
                        },
                        {
                          n: "03",
                          title: "Build Modular Assets",
                          desc: "Create scalable systems that maintain identity across jurisdictions.",
                        },
                        {
                          n: "04",
                          title: "Deploy & Optimize",
                          desc: "Launch with strategic partners and gather player data.",
                        },
                        {
                          n: "05",
                          title: "Scale IP Library",
                          desc: "Each game strengthens our portfolio and expands deployment footprint.",
                        },
                        {
                          n: "06",
                          title: "Long-Term Partners",
                          desc: "We focus on sustained collaboration, not one-off launches.",
                        },
                      ].map((s) => (
                        <div key={s.n} className="process-tile">
                          <div className="process-tile__top">
                            <div className="process-tile__label">PROCESS</div>
                            <div className="process-tile__num">{s.n}</div>
                          </div>

                          <div className="process-tile__title">{s.title}</div>
                          <div className="process-tile__desc">{s.desc}</div>

                          {/* micro glitch bars */}
                          <div className="process-tile__glitch process-tile__glitch--a" />
                          <div className="process-tile__glitch process-tile__glitch--b" />
                        </div>
                      ))}
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </section>

          {/* divider */}
          <div
            className="my-16 h-px w-full"
            style={{ background: "var(--paperBorder)" }}
          />

          {/* Looking Forward */}
          <div className="grid gap-10 lg:grid-cols-12 lg:items-start">
            <div className="lg:col-span-5">
              <h2 className="text-4xl font-semibold tracking-tight">
                Looking Forward
              </h2>
              <p
                className="mt-6 text-lg leading-8"
                style={{ color: "var(--paperMuted)" }}
              >
                Our studio roadmap moves from original e-instant titles to larger, systemic lottery platforms.
              </p>
              <p
                className="mt-4 text-lg leading-8"
                style={{ color: "var(--paperMuted)" }}
              >
                Each product builds toward a cohesive portfolio of concept-driven games designed to become recognizable, ownable IP within each jurisdiction.
              </p>
            </div>

            <div className="lg:col-span-7">
              <div className="space-y-8">
                <div className="flex items-start gap-4">
                  <span
                    className="mt-2 inline-block h-3 w-3"
                    style={{ borderRadius: 0, background: "var(--accent)" }}
                  />
                  <div>
                    <p className="text-xl font-semibold leading-8">
                      How We Compete
                    </p>
                    <p
                      className="mt-3 text-base leading-7"
                      style={{ color: "var(--paperMuted)" }}
                    >
                      Most studios compete on volume, speed, and familiarity. Hex competes on originality, intuition, and longevity. When we design games, we build them to last.
                    </p>
                  </div>
                </div>

                <div className="flex items-start gap-4">
                  <span
                    className="mt-2 inline-block h-3 w-3"
                    style={{ borderRadius: 0, background: "var(--accent)" }}
                  />
                  <div>
                    <p className="text-xl font-semibold leading-8">
                      Who We Build For
                    </p>
                    <p
                      className="mt-3 text-base leading-7"
                      style={{ color: "var(--paperMuted)" }}
                    >
                      Hex works with state and national lotteries, iLottery platforms and aggregators, and strategic distribution partners. We focus on long-term collaboration, not one-off launches.
                    </p>
                  </div>
                </div>

                <div className="flex flex-wrap gap-3">
                  <a
                    href="/games"
                    className="inline-flex items-center justify-center border px-6 py-3 text-sm font-medium"
                    style={{
                      borderRadius: 0,
                      borderColor: "rgba(0,255,140,0.45)",
                      background:
                        "linear-gradient(180deg, rgba(0,0,0,0.40), rgba(0,0,0,0.18))",
                      color: "rgba(220,255,235,0.95)",
                      boxShadow: "0 0 0 1px rgba(0,255,140,0.14) inset",
                    }}
                  >
                    View Cash Clock
                  </a>

                  <a
                    href="/contact"
                    className="inline-flex items-center justify-center border px-6 py-3 text-sm font-medium"
                    style={{
                      borderRadius: 0,
                      borderColor: "var(--paperBorder)",
                      color: "var(--paperText)",
                      background: "transparent",
                    }}
                  >
                    Contact
                  </a>

                  <a
                    href="https://hex-perpetual.com"
                    target="_blank"
                    rel="noreferrer"
                    className="inline-flex items-center justify-center border px-6 py-3 text-sm font-medium"
                    style={{
                      borderRadius: 0,
                      borderColor: "var(--paperBorder)",
                      color: "var(--paperMuted)",
                      background: "transparent",
                    }}
                  >
                    Chance Lottery (demo)
                  </a>
                </div>

                <p className="text-sm" style={{ color: "var(--paperMuted2)" }}>
                  Note: The demo link is external. If you prefer, we can route it
                  through an internal “Future Systems” page later.
                </p>
              </div>
            </div>
          </div>
        </div>
      </section>

      {/* END CAP (back to dark, like homepage) */}
      <section className="px-6 py-16" style={{ background: "rgba(0,0,0,0.88)" }}>
        <div className="mx-auto max-w-6xl">
          <div className="grid gap-8 lg:grid-cols-12 lg:items-center">
            <div className="lg:col-span-8">
              <h3
                className="text-3xl font-semibold tracking-tight sm:text-4xl"
                style={{ color: "rgba(255,255,255,0.90)" }}
              >
                Hex Game Studio exists to raise the standard of iLottery content.
              </h3>
              <p
                className="mt-4 text-lg leading-8"
                style={{ color: "rgba(255,255,255,0.72)" }}
              >
                Original mechanics. Intuitive systems. Built for longevity.
              </p>
            </div>
            <div className="lg:col-span-4 lg:text-right">
              <a
                href="/contact"
                className="inline-flex items-center justify-center px-7 py-3 text-sm font-medium"
                style={{
                  borderRadius: 0,
                  background: "var(--cta-bg)",
                  color: "var(--cta-fg)",
                }}
              >
                Start a conversation
              </a>
            </div>
          </div>
        </div>
      </section>
    </main>
  );
}